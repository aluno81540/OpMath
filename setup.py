import os

from setuptools import setup, find_packages
setup(
    name="OpMath",
    version="0.1",
    description="Frequencia 2 OpMath package",
    packages=find_packages(),
    include_package_data=True
)